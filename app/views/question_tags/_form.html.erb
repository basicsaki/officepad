<%= form_with(model: question_tag, local: true) do |form| %>
  <% if question_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_tag.errors.count, "error") %> prohibited this question_tag from being saved:</h2>

      <ul>
      <% question_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.number_field :question_id, id: :question_tag_question_id %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.number_field :tag_id, id: :question_tag_tag_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
