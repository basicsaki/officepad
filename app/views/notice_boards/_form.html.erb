<%= form_with(model: notice_board, local: true) do |form| %>
  <% if notice_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notice_board.errors.count, "error") %> prohibited this notice_board from being saved:</h2>

      <ul>
      <% notice_board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :notice_board_user_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :notice_board_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :notice_board_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
